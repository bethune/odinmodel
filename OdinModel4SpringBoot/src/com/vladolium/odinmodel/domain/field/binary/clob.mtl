[comment encoding = UTF-8 /]
[module clob('http://www.eclipse.org/uml2/5.0.0/UML')]

[template public clob(aClass : Class)]

[comment : For all fields of the current entity./]
[for (p: Property | aClass.getAllAttributes()->removeAll(aClass.getInheritedMembers())) separator('\n')]
	[if p.type.name = 'Clob']
		[if p.lower = 1 and p.isID = false] [comment : Generate the not null constraint./]
@NotNull
		[/if]
@Lob
private String [p.name/]; [comment : Define the field's type and name./]

[comment : Get and Set/]
public String get[p.name.toUpperFirst()/]() {
	return [p.name/];
}

public void set[p.name.toUpperFirst()/](String [p.name/]) {
	this.[p.name/] = [p.name/];
}
	[/if]
[/for]

[/template]
